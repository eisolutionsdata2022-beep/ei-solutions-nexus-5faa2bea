<Window x:Class="EISolutions.IppbAgent.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EI SOLUTIONS — Sign in" Height="320" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="EI SOLUTIONS IPPB Agent" FontSize="20" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Text="Sign in with your retailer account" Margin="0,4,0,16" Opacity="0.7"/>
        <TextBox Grid.Row="2" x:Name="EmailBox" Margin="0,0,0,8"/>
        <PasswordBox Grid.Row="3" x:Name="PasswordBox"/>
        <TextBlock Grid.Row="4" x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap" VerticalAlignment="Bottom"/>
        <Button Grid.Row="5" Content="Sign in" Height="36" Click="SignIn_Click"/>
    </Grid>
</Window>
