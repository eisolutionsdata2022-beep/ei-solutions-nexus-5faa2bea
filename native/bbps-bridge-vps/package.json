{
  "name": "bbps-bridge-vps",
  "version": "1.0.0",
  "private": true,
  "description": "EI SOLUTIONS — HMAC-authenticated proxy that forwards Bharat Connect (AceNeoBank) API calls through a static IP.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0"
  }
}
