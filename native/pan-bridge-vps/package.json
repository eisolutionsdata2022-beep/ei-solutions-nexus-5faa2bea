{
  "name": "ei-pan-bridge",
  "version": "1.0.0",
  "description": "Static-IP HMAC-signed proxy for mallikacyberzone PAN API. Forwards requests from Lovable Workers to the upstream provider so that whitelisted-IP restrictions are satisfied.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0"
  },
  "engines": {
    "node": ">=18.18"
  }
}
